import React from 'react';
import PropTypes from 'prop-types';

const SelectInput = (props) => {
    return (
      <h1>SelectInput Component</h1>
    );
};

SelectInput.propTypes = {
    //myProp: PropTypes.Object.isRequired,
};

export default SelectInput;
